¿Y qué pasa con la colección original, como `algunos_numeros` o `juegos`? ¿Se modifica al aplicar `select`? :cold_sweat: 

¡No, para nada! El `select`, al igual que el `filter` de funcional, **no** produce efecto. 